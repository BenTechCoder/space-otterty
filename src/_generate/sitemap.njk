---
permalink: /sitemap.xml
eleventyExcludeFromCollections: true
---
{# Excellent sitemap generation strategy from https://github.com/5t3ph/ and directly taken from https://github.com/5t3ph/11ty-netlify-jumpstart/ #}
<?xml version="1.0" encoding="utf-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{%- for page in collections.all %}
  {% set absoluteUrl %}{{ page.url | url | absoluteUrl(meta.url) }}{% endset %}
  <url>
    <loc>{{ absoluteUrl }}</loc>
		{% if page.url == '/' %}
    <lastmod>{{ collections.all | rssLastUpdatedDate }}</lastmod>
    {% else %}
    <lastmod>{{ page.date | rssDate }}</lastmod>
		{% endif %}
  </url>
{%- endfor %}
</urlset>